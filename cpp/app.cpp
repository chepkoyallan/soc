#include <iostream>

int main(){

    int desired_max_worker;
    std::cout << "Enter the desires number of workers: \n";

    std::cin >> desired_max_worker;

    std::cout << desired_max_worker;
    return 0;
}